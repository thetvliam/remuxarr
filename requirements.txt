fastapi==0.140.0
uvicorn[standard]==0.51.0
sqlalchemy==2.0.51
pydantic==2.13.4
pydantic-settings==2.14.2

# Required explicitly now. FastAPI 0.111 bundled python-multipart as a hard
# dependency, so it was pulled in implicitly; current FastAPI only pulls it
# via the [standard] extra. Two endpoints accept file uploads
# (backup import, settings import), and FastAPI raises at STARTUP —
# "Form data requires python-multipart" — the moment a form/File route is
# registered without it. So this pin is what keeps the container able to
# boot, not an optional add-on.
python-multipart==0.0.32
